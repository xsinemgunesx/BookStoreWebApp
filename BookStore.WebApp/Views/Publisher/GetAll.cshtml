@model IEnumerable<BookStore.WebApp.Models.Domain.Publisher>
<div class="container pt-5">
	<div class="row">
		<div class="col-md-6">
			<a class="btn btn-dark" href="/Publisher/Add">Add more</a>
		</div>
	</div>
	<div class="row">
		<div class ="col-md-6">
			<h3>Yazarlar</h3>
			<table class="table table-striped table-bordered table-dark">
				<thead>
					<tr>
						<th>İsim</th>
						<th></th>
					</tr>
				</thead>
				<tbody>
					@foreach(var item in Model)
					{
						<tr>
							<td>@item.Name</td>
							<td>
								<a class="btn btn-success" href="/Publisher/Update?id=@item.Id"><i class="bi bi-pen"></i>Düzenle</a>
								<a class="btn btn-danger" 
								onclick="return window.confirm('Are you sure?')"
								   href="/Publisher/Delete?id=@item.Id"><i class="bi bi-trash"></i>Sil</a>
							</td>
						</tr>
					}
				</tbody>
			</table>
		</div>
	</div>
</div>

